# how to update vm build after applying changes to ps1 scripts and other files lik autounattend.xml


```./buildandinstall.sh --iso-only```


das wird die autounattend.iso neubauen und an die korrekten stellen im code kopieren. diese datei beinhaltet die programme und scripte die während der installation von windows benötigt werden